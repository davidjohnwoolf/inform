<h2>{{title}}</h2>

<h4>Title: {{feed.title}}</h4>
<h6>ID: {{feed._id}}</h6>

<a href="/{{userId}}/feeds/{{feed._id}}/edit">Edit Feed</a>
<form onsubmit="return confirm('Are you sure you want to delete this feed?')" action="/{{userId}}/feeds/{{feed._id}}" method="post" enctype="application/x-www-form-urlencoded">
  <input type="hidden" name="_method" value="delete">
  <input type="submit" class="delete-button" value="Delete Feed">
</form>
